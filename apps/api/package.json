{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --target bun --outdir dist",
    "check-types": "tsc --noEmit",
    "dev": "bun run --env-file ../../.env.local --hot src/index.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "@xd/db": "*",
    "@xd/env": "*",
    "@xd/shared": "*",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@xd/eslint-config": "*",
    "@xd/typescript-config": "*",
    "eslint": "^9",
    "typescript": "^5"
  }
}
